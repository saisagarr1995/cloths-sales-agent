# Secrets — NEVER commit. Real values live only in .env (use .env.example as template)
.env
.env.example

# Owner-private files — keep local only
RECREATE_PROMPT.md

# WhatsApp session credentials (equivalent to being logged in as the business number)
auth/

# Private customer & order data
data/customers/
data/orders.json

# Dependencies & noise
node_modules/
*.log
